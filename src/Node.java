public class Node {
    public String node;
    public Node leftNode;
    public Node rightNode;

    public Node(String mid, Node left, Node right) {
        node = mid;
        leftNode = left;
        rightNode = right;
    }
}
